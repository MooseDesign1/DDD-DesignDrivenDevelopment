# Conventions
> Auto-generated by `/ds-init`. Refreshed by `/ds-update`.
> Inferred from scanning the Figma file. Confidence levels noted per convention.

---

## Naming Conventions

### Token naming — high confidence
- Pattern: `category/subcategory` (forward-slash separator, lowercase)
- Examples: `general/background`, `sidebar/sidebar border`, `heading 1/font-size`
- Two-tier: `absolute/*` (raw primitives) → `semantic/*` (named aliases)
- Unofficial tokens use prefix `unofficial/` — extended beyond the shadcn base

### Component naming — high confidence
- Figma pages use Title Case (e.g., `Button`, `Alert Dialog`, `Select & Combobox`)
- No prefix or namespace on component names
- Internal/utility components live in the `Internal Components` page

---

## Token Architecture

### Two-tier system — high confidence
1. **Primitive layer** (`absolute/*`, `raw colors`) — raw numeric values, all Tailwind-scale multiples of 4px
2. **Semantic layer** (`semantic/*`, `general/*`) — named aliases that reference primitives

### Multi-mode — high confidence
- Collections have two or three modes: `your_brand` (active), `shadcn` (light ref), `shadcn-dark` (dark ref)
- The `your_brand` mode is the live theme for Claude Orchestrate
- shadcn modes are reference only — do not use them for new component designs

### Brand theme characteristics — high confidence
- **Dark-first**: background is `#262626`, not white
- **Warm neutrals**: MooseTrade Neutral palette has warm/olive tones (not pure gray)
- **Teal primary**: `#02536D` (Teal-500), hover `#016687` (Teal-600)
- **Subtle surfaces**: card = background (#262626), accent = #1A1813 (nearly black)
- **Ghost interactions**: `#FFFFFF1A` (white at 10% opacity) for hover states

---

## Component Structure

### Organization — high confidence
- Components are organized as Figma **pages** (not registered in the component library)
- One page per component, named exactly after the component
- Foundation pages: Colors, Typography, Icons, Shadows
- Docs pages: About & documentation, Changelog, Credits
- Screen examples page for full-screen compositions
- `Internal Components` page for shared primitives not exposed to users

### Composition — medium confidence (inferred from page names)
- Compound components detected by name: `Button Group`, `Input Group`, `Input File`, `Toggle & Toggle Group`
- Likely parent/child: `Field` wraps `Input`/`Textarea`/`Checkbox`/`Radio`/`Select`

---

## Typography

### Font stack — high confidence
- **Body & headings**: Nunito (your_brand) — round, friendly sans-serif
- **Serif**: Playfair Display (your_brand) — editorial accent
- **Monospace**: Geist Mono (shared with shadcn)
- Note: shadcn mode uses Geist for sans, but your_brand overrides to Nunito

### Type scale — high confidence
- H1: 48/48px, -1.5px tracking, Semibold
- H2: 30/30px, -1px tracking, Semibold
- H3: 24/28.8px, -1px tracking, Semibold
- H4: 20/24px, 0px tracking, Semibold
- Body regular: 16/24px, Regular
- Body small: 14/21px, Regular
- Body mini: 12/16px, Regular

---

## Spacing

### Scale — high confidence
- Follows 4px base grid (Tailwind convention)
- Semantic names map T-shirt sizes: 3xs=2, 2xs=4, xs=8, md=16, lg=20, xl=24, 2xl=32, 3xl=40, 4xl=48, 5xl=64
- Absolute tokens use Tailwind naming (0, 0.5, 1, 1.5, 2…) multiplied by 4px

---

## Border Radius

### Scale — high confidence
- Semantic names follow Tailwind: none, xs, sm, md, lg, xl, 2xl, 3xl, full
- Default card/panel radius: likely `rounded-lg` (8px) based on shadcn defaults
- Inputs likely `rounded-md` (6px)
